import { StopcheckDirective } from './stopcheck.directive';

describe('StopcheckDirective', () => {
  it('should create an instance', () => {
    const directive = new StopcheckDirective();
    expect(directive).toBeTruthy();
  });
});
